import { Stack } from "expo-router"

const StackLayout = () => {
    return (
        <Stack>
            <Stack.Screen name="(navigation-bar)" />
        </Stack>
    )
}

export default StackLayout;